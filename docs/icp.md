# ICP — moved

The ICP is per-client now. See `clients/<active-client>/icp.md`
(e.g. `clients/smart-ai-workspace/icp.md`). Resolve the active client from
`ACTIVE_CLIENT` in `.env`, or a `client=<slug>` invocation arg. See CLAUDE.md > Active client.
