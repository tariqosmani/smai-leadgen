# Portfolio proof points — moved

The portfolio proof points and the "what NOT to claim yet" rules are per-client now.
See `clients/<active-client>/offer.md` > Portfolio proof points
(e.g. `clients/smart-ai-workspace/offer.md`). Resolve the active client from
`ACTIVE_CLIENT` in `.env`, or a `client=<slug>` invocation arg. See CLAUDE.md > Active client.
