# secrets
.env
.env.local
*.local
.claude/settings.local.json
.mcp.json

# scratch
tmp/
*.log
__pycache__/
*.pyc
.claude/worktrees/

# lead data (keep the system, not the leads — real prospect PII, never push)
data/*.json
data/exports/
data/*-export.csv
data/clickup-leads-export.csv
clients/*/linkedin-heyreach-*.csv

# per-client runtime records, written by the skills at run time (not config)
# suppress.md = unsubscribed prospect emails (PII)
clients/*/suppress.md
clients/*/report-log.md
clients/*/infrastructure.md
clients/*/handover-inbox-setup-*.md

# private per-client config kept off GitHub (e.g. pricing-ranges.local.md)
clients/*/*.local.md
