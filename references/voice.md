# Voice register — moved

The voice register and writing samples are per-client now. See
`clients/<active-client>/voice.md` (e.g. `clients/smart-ai-workspace/voice.md`).

The hard voice rules that apply to every client (no em dashes, no AI filler,
founder-led honesty) live in CLAUDE.md > Guardrails and
`docs/outreach-playbook.md` > Voice rules.
