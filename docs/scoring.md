# ICP Score rubric — moved

The 0–100 scoring rubric is per-client now. See `clients/<active-client>/scoring.md`
(e.g. `clients/smart-ai-workspace/scoring.md`). Resolve the active client from
`ACTIVE_CLIENT` in `.env`, or a `client=<slug>` invocation arg. See CLAUDE.md > Active client.
